box: kjlubick/eclipse-kepler-openjdk7@0.0.2
build:
  steps:
    - script:
        name: show me versions
        code: |
            java -version
            ant -version
            git --version
            curl --version
            pwd
            ls
            ls -lh ~
            ls -lh ~/eclipse-kepler